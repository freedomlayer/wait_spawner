
[package]
name = "wait_spawner"
version = "0.1.0"
authors = ["real <real@freedomlayer.org>"]
edition = "2018"

[dependencies]

futures-preview = {version = "0.3.0-alpha.13", features = ["compat"] }
backtrace = "0.3.14"
